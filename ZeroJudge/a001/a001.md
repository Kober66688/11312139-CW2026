# 題目
a001. 哈囉

# 解題思路
使用 `sys.stdin` 迴圈連續讀取系統輸入的每一行資料，透過 `.strip()` 移除前後多餘的換行符號。最後利用 Python 的 f-string 格式化字串，在輸入的文字前加上 "hello, " 並印出。

# 解題編號
18077431

# 程式碼
```python
import sys

for line in sys.stdin:
    name = line.strip()
    print(f"hello, {name}") 
